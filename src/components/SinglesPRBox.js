import React from "react"

export default function SinglesPRBox({ children }) {
  return (
    <div className="box has-text-dark has-text-left content singles-pr-box">
      {children}
    </div>
  )
}
